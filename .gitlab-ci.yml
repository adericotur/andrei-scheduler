image: node:latest
stages:
  - build
  - test
  - deploy

before_script:
  - npm install
  - npm --prefix ./functions install

pages:
  stage: deploy
  artifacts:
    paths:
      - dist/
  script:
    - npm run build
  only:
    - master
